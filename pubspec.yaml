# lib/pubspec.yaml
name: my_flutter_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0+5 # camera 패키지 추가
  path: ^1.8.3
  googleapis: ^11.4.0
  googleapis_auth: ^1.4.1
  image: ^4.0.17  # 이미지 처리를 위한 패키지
  path_provider: ^2.0.15  # 임시 파일 저장을 위한 패키지
  provider: ^6.0.5  # 상태 관리를 위한 provider 추가
  get_it: ^7.6.0   # 의존성 주입을 위한 get_it 추가
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/sesac-24-109.json
    - assets/images/

  